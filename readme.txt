Post unzipping the folder, do the following to the app locally

$npm install

Open another terminal and run the redis server

$redis-server

Run the dev instance

$npm run dev 

To build the app

$npm run build

To start the app

$npm run start

